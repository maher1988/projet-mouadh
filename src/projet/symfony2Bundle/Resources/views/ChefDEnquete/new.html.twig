{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ChefDEnquete creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('chefdenquete') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
