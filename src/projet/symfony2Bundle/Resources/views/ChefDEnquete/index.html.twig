{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ChefDEnquete list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cin</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Gouvernorat</th>
                <th>Telephone</th>
                <th>Adress</th>
                <th>Email</th>
                <th>Password</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('chefdenquete_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cin }}</td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.prenom }}</td>
                <td>{{ entity.gouvernorat }}</td>
                <td>{{ entity.telephone }}</td>
                <td>{{ entity.adress }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.password }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('chefdenquete_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('chefdenquete_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('chefdenquete_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
